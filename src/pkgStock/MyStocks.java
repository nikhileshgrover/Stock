package pkgStock;

import java.io.IOException;
import java.util.HashMap;


public class MyStocks {

	/*
	private HashMap<String, String> propertyConfigs = new HashMap<>();
	private GetPropertyValues property = new GetPropertyValues();
	
	public static void main(String[] args) {
		try {
			//Load the property file
			MyStocks stk = new MyStocks();
			stk.loadPropConfigs();
		
			//Get Stock Data in a List Object
			DataFileManager dm = new DataFileManager();
			dm.initializeDataFile(stk.propertyConfigs);
			Stocks stockData = dm.getFileData(stk.propertyConfigs);
//			System.out.println(stockData.getStocks().size());
//			System.out.println(stockData.getStocks().get(0).getSector());
			
			//Build Form
			FormBuilder formBuilder = new FormBuilder();
			formBuilder.launchForm(stockData);
			
		}catch (IOException io) 
		{
			//create common logging library	
		}
		
	}
	
	private void loadPropConfigs() throws IOException {
		propertyConfigs = property.getPropValues();
	}*/

}
